services:
  - type: web
    name: lm-video-downloader
    runtime: node
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y python3 python3-pip ffmpeg
      pip3 install yt-dlp --break-system-packages
      npm install
    startCommand: node server.js